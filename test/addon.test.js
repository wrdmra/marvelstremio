import assert from "node:assert/strict";import { after, before, test } from "node:test";import {  buildManifest,  catalogData,  createServer,  parseCatalogPath,} from "../main.js";let server;let baseUrl;before(async () => {  server = createServer();  await new Promise((resolve) => server.listen(0, "127.0.0.1", resolve));  const { port } = server.address();  baseUrl = `http://127.0.0.1:${port}`;});after(async () => {  await new Promise((resolve, reject) =>    server.close((error) => (error ? reject(error) : resolve())),  );});test("manifest defines exactly the three requested catalogs", () => {  const manifest = buildManifest("https://example.test");  assert.deepEqual(    manifest.catalogs.map(({ type, id }) => ({ type, id })),    [      { type: "series", id: "marvel-series" },      { type: "movie", id: "marvel-movies" },      { type: "movie", id: "mcu-chronological" },    ],  );  assert.deepEqual(manifest.resources, ["catalog"]);});test("all catalog entries use matching types and IMDb IDs", () => {  for (const meta of catalogData["marvel-series"]) {    assert.equal(meta.type, "series");    assert.match(meta.id, /^tt\d+$/);  }  for (const catalogId of ["marvel-movies", "mcu-chronological"]) {    for (const meta of catalogData[catalogId]) {      assert.equal(meta.type, "movie");      assert.match(meta.id, /^tt\d+$/);    }  }});test("MCU catalog follows chronological timeline order", () => {  assert.deepEqual(    catalogData["mcu-chronological"].slice(0, 3).map(({ name }) => name),    ["Captain America: The First Avenger", "Captain Marvel", "Iron Man"],  );  assert.deepEqual(    catalogData["mcu-chronological"].slice(-3).map(({ name }) => name),    [      "Captain America: Brave New World",      "Thunderbolts*",      "The Fantastic Four: First Steps",
